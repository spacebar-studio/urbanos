import React from "react";
import { createRoot } from "react-dom/client";
import UrbanOS from "../UrbanOS.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UrbanOS />
  </React.StrictMode>
);
